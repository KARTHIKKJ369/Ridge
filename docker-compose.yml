services:
  ridge:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ridge-crag
    ports:
      - "7860:7860"
    env_file:
      - path: .env
        required: false
    environment:
      - GROQ_API_KEY=${GROQ_API_KEY:-}
      - GROQ_MODEL=${GROQ_MODEL:-qwen/qwen3.6-27b}
      - GROQ_FAST_MODEL=${GROQ_FAST_MODEL:-qwen/qwen3.6-27b}
      - EMBEDDING_MODEL=${EMBEDDING_MODEL:-BAAI/bge-large-en-v1.5}
    volumes:
      - chroma_data:/app/chroma_db
    restart: unless-stopped

volumes:
  chroma_data:
